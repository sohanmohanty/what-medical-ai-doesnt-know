# Processed Data

This directory is reserved for derived datasets that are safe to recreate from the raw public benchmarks.

The current project keeps most reproducible outputs in `results/` rather than committing large processed feature matrices here.
